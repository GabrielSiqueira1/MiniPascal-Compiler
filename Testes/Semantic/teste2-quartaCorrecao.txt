program teste2

        a is int;
        b, c, d, var is float;
begin
    read (a);
    b = a * a;
    c = b + (a/2) * (35.0/b);
    write (c);
    var = 34.2;
    c = var + 2.2 + a;
    write (var)
end.